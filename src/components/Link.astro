---
interface Props {
  text: string;
  href?: string;
}

const { text, href = '#' } = Astro.props;
---

<a
  href={href}
  class="text-default text-brand-gray border-brand-border hover:text-brand-blue hover:border-brand-blue border px-5 pt-1.75 pb-2 transition-colors"
  >{text}</a
>
